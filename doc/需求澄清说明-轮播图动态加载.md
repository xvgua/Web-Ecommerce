# 轮播图（Banner）动态加载 — 需求澄清

> 编写日期：2026-06-05
> 说明：管理后台轮播 CRUD 已完整实现，本文档聚焦客户端从后端动态获取轮播数据。

---

## 现状

| 层面 | 状态 |
|------|------|
| 数据库 `banner` 表 | 已存在，含 `title / image_url / link_url / sort_order / status` |
| 后端 Admin Controller | 已存在（`/api/admin/banners`），CRUD + 状态切换 + 图片上传 |
| 管理后台页面 | 已存在（列表 + 表单），支持上传图片、设置链接、排序 |
| 后端用户端 API | **缺失** — 无公开 `/api/banners` 端点供首页获取数据 |
| 客户端首页轮播 | **硬编码** 3 条数据（CSS 渐变背景 + 文字），未从后端获取 |

---

## 1. 客户端数据获取

**Q1: 客户端从哪个接口获取轮播数据？**
- A. 新增公开接口 `GET /api/banners`，返回所有 `status=1`（启用）的轮播，按 `sort_order` 升序
- A. 新增公开接口 `GET /api/banners`，返回所有 `status=1`（启用）的轮播，按 `sort_order` 升序，最多 6 条 ✅
- ~~B. 复用管理后台~~（需登录，不适用）
- C. 其他方案

**Q2: 是否只展示启用的轮播？**
- A. 是，仅 `status=1` 的轮播出现在首页 ✅
- B. 否，管理员可以控制哪些显示/隐藏
- C. 加一个"首页展示"独立开关

**Q3: 轮播数量上限？**
- A. 不限制
- B. 限制最多 N 条（6 条），超出时后端只返回前 N 条 ✅
- C. 前端只展示前 N 条，后端返回全部

---

## 2. 轮播图内容与样式

**Q4: 轮播图的展示形式？**

- A. 图片模式：每个轮播为一张完整设计图，点击可跳转 `link_url`。无文字叠加层。 ✅
- B. 保留渐变 + 文字模式
- C. 混合模式

> `link_url` 为空则不跳转，cursor 不显示 pointer。

**Q5: 轮播图尺寸？**
- A. 固定高度 400px，宽度自适应，图片 `object-fit: cover` 居中裁剪 ✅
- B. 管理员上传时指定尺寸
- C. 保持上传图片原始比例

**Q6: 点击轮播图后的行为？**
- A. 有 `link_url` 则当前窗口跳转，无链接则不可点击 ✅
- B. 始终不可点击（纯展示）
- C. 点击跳转商品分类/搜索页

---

## 3. 首页布局适配

**Q7: 轮播图占首页的位置？**
- A. 保持当前布局：左侧分类菜单 + 中间轮播 + 右侧快捷入口 ✅
- B. 轮播全宽，去掉左右两栏
- C. 仅调整轮播区域

**Q8: 轮播数量变化时轮播指示器如何展示？**
- A. 始终显示指示器
- B. 仅 1 条轮播时隐藏指示器和箭头，多条时正常显示 ✅
- C. 超过 1 条时显示指示器

---

## 4. 后端实现

**Q9: 客户端接口设计？**
- A. 新增 `BannerController`（`/api/banners`），一个端点 ✅：
  - `GET /api/banners` — 返回 `status=1` 的 Banner 列表，按 `sort_order` 升序，最多 6 条
- B. 在现有 Controller 中新增方法

**Q10: 返回字段？（按推荐）**

| 字段 | 说明 |
|------|------|
| id | 轮播ID |
| title | 标题（用于 SEO / alt 文本） |
| imageUrl | 图片地址 |
| linkUrl | 跳转链接 |
| sortOrder | 排序 |

---

## 5. 前端实现

**Q11: 客户端 API 文件？**
- A. 新建 `Front/client/src/api/banner.ts` ✅
- B. 直接写在组件内

**Q12: 图片加载失败处理？**
- A. 显示默认占位图 ✅（用 `el-image` error slot）
- B. 跳过该轮播，不显示
- C. `el-image` 自带错误处理

**Q13: 轮播无数据时？**
- A. 隐藏整个轮播区域
- B. 显示占位图 / 默认轮播 ✅
- C. 不处理

---

## 6. 决策汇总

| # | 决策 | 要点 |
|---|------|------|
| Q1 | A | 新增公开 GET /api/banners，最多 6 条 |
| Q2 | A | 仅 status=1 展示 |
| Q3 | B | 最多 6 条 |
| Q4 | A | 纯图片模式 |
| Q5 | A | 固定高度 400px，cover 裁剪 |
| Q6 | A | 有 link_url 则跳转 |
| Q7 | A | 保持三栏布局 |
| Q8 | B | 单条时隐藏指示器 |
| Q9 | A | 新建公开 BannerController |
| Q10 | 推荐 | id / title / imageUrl / linkUrl / sortOrder |
| Q11 | A | 新建 api/banner.ts |
| Q12 | A | 占位图兜底 |
| Q13 | B | 无数据时显示默认轮播（占位） |

---

## 7. 实施范围

| 模块 | 内容 | 状态 |
|------|------|------|
| 管理后台 | CRUD + 图片上传 + 排序 + 状态切换 | 已实现 ✅ |
| 后端公开 API | `GET /api/banners`（公开，最多 6 条，仅 status=1） | 待实现 |
| 客户端首页 | 替换硬编码数据为 API 调用 | 待实现 |
| 客户端 API | `api/banner.ts` | 待新建 |
| 拦截器配置 | `/api/banners/**` 放行 | 待修改 |

---
