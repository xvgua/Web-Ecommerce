# 查看物流功能 — 初步审查报告

## 一、需求理解

用户可在订单列表页对 **"待收货"（status=2）** 和 **"已完成"（status=3）** 的订单点击"查看物流"按钮，进入独立的物流详情页，页面布局参考淘宝物流详情页，但视觉风格与本系统（Element Plus + 白底圆角卡片）保持一致。

---

## 二、参考页面分析（查看物流页面.html）

该文件是淘宝 PC 端的物流详情页存档（约 456KB，含大量 base64 图片和 JS）。核心页面结构如下：

```
┌─ 物流详情 ──────────────────────────────────────────┐
│                                                       │
│  ┌─ 包裹信息 (pc-package-wrapper) ──────────────────┐ │
│  │  [商品图] 已下单 [商品名]                          │ │
│  │           [旺旺] 联系商家                          │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌─ 物流公司 (pc-company-wrapper) ──────────────────┐ │
│  │  [Logo] 物流公司名称    运单号：SF1234567890      │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌─ 物流轨迹 (pc-node-wrapper) ─────────────────────┐ │
│  │  ● 已发货                          05-16 10:57   │ │
│  │  │  [详细信息描述]                                │ │
│  │  ● 快递已揽收                      05-16 11:30   │ │
│  │  │  [详细信息描述]                                │ │
│  │  ○ 运输中                              --        │ │
│  │  │                                               │ │
│  │  ○ 派送中                              --        │ │
│  │  │                                               │ │
│  │  ○ 已签收                              --        │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌─ 收货地址 (pc-address-wrapper) ──────────────────┐ │
│  │  📍 收货地址: 许**, 177****7453, 辽宁省沈阳市...  │ │
│  └─────────────────────────────────────────────────┘ │
│                                                       │
│  ┌─ 猜你喜欢（推荐商品） ────────────────────────────┐ │
│  └─────────────────────────────────────────────────┘ │
└───────────────────────────────────────────────────────┘
```

**视觉特征:**

- **垂直时间线**：左侧竖线 + 圆点（已完成=实心蓝色，进行中=绿色大圆点，未完成=空心灰色）
- **节点内容**：右侧分两行（第一行=状态描述 + 时间，第二行=详细说明/地点）
- **快递公司区**：Logo + 公司名 + 运单号
- **收货地址**：定位图标 + 脱敏手机号 + 完整地址

---

## 三、当前系统现状

### 3.1 已有但未完成的部分

| 位置 | 现状 |
|------|------|
| `list.vue:68` | 已有"查看物流"按钮，但**仅对 status=2 (待收货) 显示**，缺少 status=3 (已完成) |
| `list.vue:195-197` | `handleViewLogistics` **直接跳转订单详情页** `/orders/${id}`，未跳转物流页 |
| `api/order.ts:57-59` | `getLogistics(id)` API 函数已定义，调 `GET /orders/${id}/logistics` |
| **路由** | 无 `/orders/:id/logistics` 路由 |
| **后端 Controller** | 无 `GET /orders/{id}/logistics` 端点 |
| **数据库 `order` 表** | 无物流相关字段（运单号、物流公司等） |

### 3.2 后端发货现状

```java
// AdminOrderController — 发货接口
PUT /api/admin/orders/{id}/ship
  → orderService.shipOrder(id)  // 仅将 status: 1→2，无运单号记录
```

`shipOrder` 方法只修改订单状态，**不记录任何物流公司、运单号等信息**。

### 3.3 订单状态枚举

```
0 = 待支付    1 = 待发货    2 = 待收货(已发货)
3 = 已完成    4 = 已取消    5 = 退款中
```

---

## 四、实现范围分析

### 4.1 必须新建/修改的内容

**后端：**

1. **数据库** — `order` 表新增字段：`tracking_no`（运单号）、`logistics_company`（物流公司），或在发货时记录
2. **Order 实体** — 新增对应字段
3. **DTO** — 新建 `LogisticsInfo` 响应对象，包含：物流公司、运单号、轨迹列表（状态+时间戳+描述）、收货地址
4. **Controller** — 新增 `GET /api/orders/{id}/logistics` 端点
5. **Service** — 实现物流查询逻辑 + 发货时生成模拟物流轨迹

**前端：**

1. **新建页面** — `views/order/logistics.vue`（物流详情页）
2. **路由** — 新增 `/orders/:id/logistics`
3. **修改 `list.vue`** — `handleViewLogistics` 改为跳转物流页，增加 status=3 的按钮
4. **共享类型** — `shared/types/order.ts` 新增 `LogisticsInfo`、`TrackingNode` 接口
5. （可选）管理员发货时可填写运单号

### 4.2 建议采用模拟物流数据方案

考虑到这是教学项目，建议：

- 管理员点"发货"后，后端自动生成模拟物流轨迹（3-5 条节点，如：已揽收 → 运输中 → 派送中 → 已签收）
- 每条轨迹包含状态描述和时间戳
- 已完成订单显示完整轨迹（最后一条为"已签收"），待收货订单显示进行中的轨迹

---

## 五、设计建议（与系统风格保持一致）

参考现有订单详情页 `detail.vue` 的设计语言：

| 设计元素 | 现有系统风格 | 物流页建议 |
|----------|-------------|-----------|
| 容器 | `max-width: 1000px` 居中 | 复用 |
| 卡片 | 白底 `border-radius: 12px`，`padding: 24px`，`margin-bottom: 16px` | 复用 |
| 标题 | `font-size: 22px`，`font-weight: 700` | 复用（"物流详情"） |
| 区块标题 | `font-size: 16px`，`font-weight: 600` | 复用（"物流轨迹""收货信息"等） |
| 时间线 | **新组件** | 左侧竖线 + 圆点，替代 `el-steps` |
| 商品展示 | 复用 `ProductImage` 组件 | 包裹信息区 |
| 地址展示 | 复用 `detail.vue` 的 `.address-box` 样式 | 收货地址区 |
| 颜色 | `#e6423a`（价格红）、`#409eff`（主色蓝）、`#f0f0f0`（边框灰）、`#fafafa`（背景灰） | 主题色统一 |
| 空状态 | `el-empty` | 无物流信息时显示 |

---

## 六、待确认事项

1. **物流数据来源** — 是模拟生成还是对接真实快递 API？（建议模拟）
2. **管理员发货流程** — 是否需要管理员在发货时填写运单号？还是自动生成？
3. **已完成订单的物流** — 是否只显示最终"已签收"状态，还是保留完整轨迹？
4. **推荐商品区** — 参考页面底部有"猜你喜欢"，是否需要实现？（建议第一版不做，简化范围）

---

## 七、工作量预估

| 部分 | 内容 | 预估复杂度 |
|------|------|-----------|
| 后端-数据模型 | Order 实体 + DB 字段 + DTO | 低 |
| 后端-物流接口 | Controller + Service（模拟轨迹） | 中 |
| 前端-物流页面 | logistics.vue（核心页面） | 中 |
| 前端-路由+按钮 | 路由配置 + list.vue 修改 | 低 |
| 前端-共享类型 | TypeScript 接口定义 | 低 |

**总评估**：中等规模，涉及后端新增接口 + 前端新增页面，预计新建 2-3 个文件，修改 5-6 个文件。
