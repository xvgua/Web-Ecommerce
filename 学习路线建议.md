# 技术栈学习路线建议

> 前置背景：你已经会 Vue 3，对其他技术不熟悉。以下是基于本项目实际情况的学习建议。

---

## 一、总体策略：从"能跑通"到"能理解"

你的目标是理解这个电商项目，而不是从零学会每项技术。所以最优策略是：

**以项目为锚点，顺着数据流学，碰到什么学什么。**

具体来说，挑一个最典型的业务场景（比如"用户注册 → 登录 → 浏览商品 → 加购物车 → 下单"），从头跟到尾，每一层遇到不懂的技术就停下来查，查完继续走。

这样做的好处：
- 不会迷失在每项技术的海量文档里
- 学到的东西立刻能在项目里验证
- A 技术和 B 技术之间的关系自然建立

---

## 二、学习优先级：三个梯队

### 第一梯队 — 不学寸步难行（1～2 天）

| 技术 | 为什么重要 | 学多深 |
|---|---|---|
| **TypeScript** | 整个前端代码都是 `.ts` / `.vue` + TS，不知道类型语法连代码都读不下去 | 能读懂 `interface`、`type`、泛型 `<T>`、`as` 断言即可 |
| **MySQL** | 后端的核心就是读写数据库，不理解表和 SQL，后端逻辑完全看不懂 | 会 `SELECT/INSERT/UPDATE/DELETE`、`WHERE`、`JOIN`、看懂建表语句 |
| **Java 基础语法** | 后端代码全是 Java，至少要能读懂类、方法、注解、泛型 | 类/对象、方法、List/Map、`@` 注解的含义 |

**做法：**

 **TypeScript**（半天）：
- 直接看官方 handbook 前 5 章：[TypeScript for the New Programmer](https://www.typescriptlang.org/docs/handbook/2/basic-types.html)
- 核心概念：`interface` 定义对象形状、`<T>` 泛型（类比 Vue 3 的 `ref<string>()`）、`?:` 可选属性
- 打开项目 `shared/types/` 目录对照着看，这就是最好的教材

**MySQL**（半天）：
- [SQLBolt](https://sqlbolt.com/) 前 10 课，交互式练习，2 小时内搞定
- 然后打开项目的 `.sql` 建表脚本或者 Entity 类，和表结构对照着看

**Java 基础**（半天）：
- 你已经有 JS/TS 基础，Java 的语法差异主要在于：强类型（变量必须声明类型）、类必须放在文件里、没有 `const`/`let` 只有类型声明
- 重点理解注解（`@Override`、`@Autowired` 等），它们是 Spring 的核心机制
- 可以直接看项目里 `entity/` 和 `dto/` 下的类，它们就是最简单的 Java 类

---

### 第二梯队 — 能看懂核心流程（3～5 天）

| 技术 | 为什么重要 | 学多深 |
|---|---|---|
| **Spring Boot 基础** | 整个后端就是 Spring Boot 驱动的，不理解它就看不懂后端怎么启动、请求怎么进来的 | 理解 IoC/DI、Controller 如何接收请求、`application.yml` 怎么配置 |
| **MyBatis-Plus** | 后端所有数据库操作都通过它，不懂就看不懂数据怎么存的 | 理解 `BaseMapper` 提供的 CRUD、`LambdaQueryWrapper` 条件查询 |
| **Axios** | 前后端数据交互的唯一通道，不懂就不知道前端怎么调后端接口 | 理解拦截器机制、请求/响应格式 |
| **Vue Router** | 页面跳转全部靠它，你 Vue 3 基础之上加这一层 | 路由定义、动态路由 `:id`、导航守卫 |

**做法：**

**Spring Boot**（1～2 天）：
1. 看 B 站/YouTube 上任意一个 Spring Boot 快速入门视频（2 小时以内那种），了解几个核心概念：
   - **IoC 容器**：Spring 帮你创建和管理对象，你只需要声明"我需要什么"
   - **`@RestController`**：标记一个类是 HTTP 接口，`@GetMapping`/`@PostMapping` 标记方法处理哪种请求
   - **`@Autowired`**：自动注入依赖，类比 Vue 的 `inject()`
   - **`application.yml`**：所有配置写在这里，Spring Boot 启动时读取
2. 然后打开项目 `controller/AuthController.java`，看 `/api/auth/login` 是怎么处理的，这是最简短的示例

**MyBatis-Plus**（半天）：
- MyBatis-Plus 的核心理念是：一个接口（Mapper）对应一张表，继承 `BaseMapper<T>` 就不需要写 SQL
- 打开 `mapper/UserMapper.java`（接口）和 `service/impl/UserServiceImpl.java`（实现），看一个查询到底是怎么完成的
- 理解 `LambdaQueryWrapper` 就是"用 Java 代码拼 WHERE 条件"

**Axios**（半天）：
- 打开 `client/src/api/request.ts`，看：
  1. `baseURL` 设成了什么
  2. 请求拦截器在做什么（自动加 token）
  3. 响应拦截器在做什么（统一错误处理）
- 然后打开 `api/product.ts`，看一个具体的 API 调用是怎么写的

**Vue Router**（半天）：
- 你已经会 Vue 3，Router 就是额外的一层：定义 URL → 组件的映射
- 打开 `client/src/router/index.ts`，对照 `App.vue` 或布局组件里的 `<router-view />`

---

### 第三梯队 — 锦上添花（按需学习）

| 技术 | 什么时候学 |
|---|---|
| **JWT 认证机制** | 当你想理解"登录到底发生了什么"，从 AuthController → JwtUtils → LoginInterceptor 跟一遍 |
| **BCrypt** | 只需要知道它是单向加密，不可逆，登录时用 `matches()` 比对 |
| **Element Plus** | 用到了就查文档：[element-plus.org](https://element-plus.org/)，不需要系统学 |
| **Pinia** | 你已经会 Vue 3 的 `ref`/`reactive`，Pinia 本质上就是把它们封装成全局共享的模块 |
| **Vite** | 你能跑起来就行，深入配置暂时不需要 |
| **Sass/SCSS** | CSS 的你肯定熟悉，SCSS 只是多了嵌套和变量，看项目代码自然就会了 |
| **Lombok** | `@Data` = 自动生成 getter/setter，看一眼就懂 |
| **ECharts** | 只在后台 Dashboard 用到，需要时再看 |
| **Maven** | `pom.xml` 就是声明"我需要哪些第三方库"，你暂时不需要自己配置 |

---

## 三、学习路径图（按场景推荐）

### 如果你只有 1 天：

```
搞清楚一个完整请求是怎么走的：

1. 前端：打开 client/src/views/user/login.vue
       → 找到表单提交时调用的 API 函数
2. 前端：打开 client/src/api/user.ts
       → 找到 login 函数，看它怎么用 axios 发 POST
3. 后端：打开 controller/AuthController.java
       → 找到 @PostMapping("/login")，看怎么接收参数、调用 Service、返回 Result
4. 后端：打开 service/impl/UserServiceImpl.java
       → 看业务逻辑：查数据库 → 比对密码 → 生成 JWT → 返回
5. 后端：打开 mapper/UserMapper.java
       → 看怎么查数据库

跟着这个链条，TypeScript/Java/Axios/Spring Boot/MyBatis-Plus/JWT 你全碰了一遍。
```

### 如果你有 3 天：

在上面 1 天的基础上，再跟两条链路：
- **浏览商品**：ProductController → ProductService → ProductMapper（含自定义搜索 SQL）
- **下单**：CartController → OrderController → OrderService（事务、库存扣减、状态流转）

然后把 `shared/types/` 下的所有类型定义通读一遍，理解这个系统有哪些数据。

### 如果你有 1 周：

在前面基础上，加上：
- 自己从零写一个简单功能（比如"用户修改昵称"），从前端页面到后端接口到数据库变更全走通
- 把后台管理系统的代码也看一遍，对比前后台的设计差异

---

## 四、关键提醒

1. **不要试图精通每一样**。这个项目的目标是把它们串起来用，不是深度学习每一项技术。比如 MyBatis-Plus，你会用 `BaseMapper` 的 CRUD + `LambdaQueryWrapper` 就已经覆盖了 90% 的场景。

2. **Java ≠ Spring Boot**。你第一眼看到 Java 代码可能会被注解吓到，`@RestController`、`@RequestMapping`、`@Autowired`、`@Data`...它们只是**标记/声明**，不是执行逻辑。直接看方法体里的代码，那才是真正的业务逻辑。

3. **TypeScript 和 Java 的语法相似性超乎你想象**。你熟悉 Vue 3 + TS，那 Java 的 `List<String>` = TS 的 `string[]`，Java 的 `Map<String, Object>` ≈ TS 的 `Record<string, any>`。利用这种对应关系加速学习。

4. **善用 Ctrl+Click**。在 VS Code 里按着 Ctrl 点一个函数名/类名，直接跳到定义处。这是理解项目最快的方式——跟着调用链一直往下跳。

5. **项目里的 `Result<T>` 是所有接口的统一外壳**。不管调哪个接口，后端返回的都是 `{ code, message, data }`，data 的类型由泛型 `<T>` 决定。这是你理解前后端数据交互的关键。
